To build and compile the svm binary follow the following steps:
  Linux: cmake, make, gcc, g++
    1: in the root directory of scc create the build/ directory, then change into that directory
    2: inside that build/ run cmake ..
    3: then run make
